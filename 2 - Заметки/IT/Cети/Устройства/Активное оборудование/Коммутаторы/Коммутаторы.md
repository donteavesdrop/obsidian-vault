2026-04-07 в 16:29
Теги:[[Активное оборудование]]

----
**Коммутатор (как компонент сети)** – устройство, обеспечивающее подключения к сети конечных систем и выполняющее интеллектуальную коммутацию данных внутри локальной сети

**Коммутатор Ethernet (как компонент локальной сети)** – образует точки агрегирования для локальных сетей. Действует на 2 уровне модели OSI и обеспечивает интеллектуальную передачу кадров внутри локальной сети
![[Pasted image 20260413160136.png]]
**Особенности коммутаторов**
![[Pasted image 20260413160152.png]]
**Основные функции коммутаторов**
• Подключение конечных устройств 
• Пересылка кадров 
• Разбиение на подсети (VLAN) 
• Поддержка отказоустойчивой топологии

**Как происходит обработка кадров коммутатором:**
Когда порт принимает индивидуальный кадр, коммутатор сравнивает МАС-адрес назначения с адресами в МАС таблице 
Если коммутатор решает что МАС-адрес назначения кадра находится в том же физическом сегменте сети, что и источник, он не пересылает кадр. 
Этот процесс называется фильтрацией, и с его помощью коммутаторы могут значительно уменьшить трафик

### Почему нужен switch

Проблема хаба: всем летит всё. Switch решает задачу на уровне L2: доставлять кадры **только нужному получателю**.

### Как switch знает, куда отправлять

Switch поддерживает таблицу соответствий (в лекции названа как **CamTable**/**MAC address table**):

- строка вида: **порт → MAC‑адрес**.
    

Когда устройство подключается и работает, switch «обучается»:

- при получении кадров от хоста он запоминает, **на каком порту** увидел источник (source MAC),
    
- затем по destination MAC выбирает **конкретный порт** для доставки.
    

> В простом сценарии: если кадр пришёл на порт 1 с **source MAC = MAC1**, switch запоминает: MAC1 связан с портом 1; дальше кадры с destination MAC2 будут уходить на порт, где сидит MAC2.

## 🌉 Каскадирование switch’ей (сложнее: несколько коммутаторов)

Лектор поставил следующий усложняющий сценарий:

- несколько switch’ей соединены **каскадно**,
    
- компьютеры могут быть подключены к разным switch’ам.
    

Вопрос: как каждый switch (имея свою CAM‑таблицу) будет корректно доставлять кадры, если нужный MAC находится «за другим коммутатором»?

Это переводит внимание к практическому поведению сети при множественных L2‑сегментах — к тому, как распространяются кадры и как обучаются/используются таблицы.
